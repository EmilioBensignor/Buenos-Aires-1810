# Logros tipo Steam — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sistema de 20 logros tipo Steam, completables al 100%, con toast al desbloquear y galería para trackear progreso, persistido en localStorage.

**Architecture:** Bus de eventos en `useGameState` (solo emite, vía `registrarEvento`/`onEvento`). Composable nuevo `useLogros.js` (singleton) con catálogo + store persistido + reglas + cola de toasts; escucha el bus y observa `state`. Dos componentes UI: `ToastLogro.vue` (cola) y `GaleriaLogros.vue` (modal). Cableado mínimo en los 5 minijuegos, garito, NPCs, TopBar y GameRoot.

**Tech Stack:** Nuxt 4 (`ssr: false`), Vue 3 composition API, Tailwind 3. Sin suite de tests en el proyecto: la lógica pura de `useLogros` se testea con node + asserts (`node script.mjs`); los componentes Vue se verifican con `node --check` + validación visual del usuario. Check de cierre: `npx nuxt build`.

**Spec:** `docs/superpowers/specs/2026-06-09-logros-design.md`

---

## Notas para quien ejecuta

- **Idioma**: código en inglés, dominio en español (igual que el resto del juego). Comentarios en español, mínimos.
- **Tailwind**: respetar el orden de clases del CLAUDE.md (sizing → layout → position → ...).
- **Centavos**: la plata está en centavos enteros. $50 = 5000, $1 = 100, apuesta máx = 500.
- **No correr dev server propio** salvo que se pida; el usuario valida en su localhost:3000.
- **No tocar `nuxt.config.js`** (dispara el 504). No hace falta para este plan.
- **Git**: NO commitear sin que el usuario lo pida. Los pasos "Commit" del plan quedan supeditados a su OK (anunciar el comando antes).
- **Patrón singleton**: `useLogros` se define a nivel módulo igual que `useGameState` (estado fuera de la función, la función solo devuelve referencias).

---

## File Structure

Nuevos:
- `app/composables/useLogros.js` — catálogo de 20 + store persistido + reglas por evento + cola de toasts + watchers de state. Singleton.
- `app/components/ui/ToastLogro.vue` — toast con cola (lee `toastActual`, avanza con `descartarToast`).
- `app/components/ui/GaleriaLogros.vue` — modal galería estilo ControlesAyuda.

Modificados:
- `app/composables/useGameState.js` — `registrarEvento`/`onEvento`; `reiniciar()` llama `reiniciarRacha()`.
- `app/components/minigames/DiceGame.vue` — emitir `jugo`/`gano`/`perdio` + `dadosExacto7`.
- `app/components/minigames/CardsGame.vue` — emitir `jugo`/`gano`/`perdio`.
- `app/components/minigames/CupsGame.vue` — emitir `jugo`/`gano`/`perdio` + `cubiletesNivel`.
- `app/components/minigames/SapoGame.vue` — emitir `jugo`/`gano`/`perdio` + `sapoX3`.
- `app/components/minigames/BingoGame.vue` — emitir `jugo`/`gano`/`perdio` + `bingoDoble`.
- `app/game/plaza/PlazaScene.js` — `hablar()` devuelve también `id`.
- `app/game/interiors/InteriorScene.js` — `hablar()` devuelve también `id`.
- `app/components/scenes/PlazaScene.vue` — emitir `hablo {npcId}` al hablar.
- `app/components/scenes/InteriorScene.vue` — emitir `hablo {npcId}` al hablar.
- `app/components/scenes/GaritoScene.vue` — emitir `saldo` al saldar.
- `app/components/ui/TopBar.vue` — botón 🏆 que emite `logros`.
- `app/components/GameRoot.vue` — montar `ToastLogro`; togglear `GaleriaLogros`.

---

## Task 1: Bus de eventos en useGameState

**Files:**
- Modify: `app/composables/useGameState.js`

El bus es un array de listeners a nivel módulo. `registrarEvento(tipo, datos)` los notifica; `onEvento(fn)` suscribe y devuelve un unsub. Esto desacopla: `useGameState` no conoce `useLogros`.

- [ ] **Step 1: Agregar el bus a nivel módulo**

En `app/composables/useGameState.js`, después de la definición de `state` (tras la línea `const NIVEL_MAX = 10` o cerca, antes de los watchers), agregar:

```js
// Bus de eventos del juego: useLogros (u otros) se suscriben con onEvento.
// useGameState solo emite; no conoce a sus consumidores.
const listeners = new Set()

function registrarEvento(tipo, datos = {}) {
  for (const fn of listeners) {
    try { fn({ tipo, ...datos }) } catch {}
  }
}

function onEvento(fn) {
  listeners.add(fn)
  return () => listeners.delete(fn)
}
```

- [ ] **Step 2: Hook para cortar la racha en partida nueva**

`useLogros` registrará un callback de "partida nueva" para cortar la racha transitoria.
Agregar a nivel módulo (junto al bus):

```js
// Callbacks que corren al empezar partida nueva (reiniciar). useLogros corta su racha acá.
const onReiniciarCbs = new Set()
function onReiniciarPartida(fn) {
  onReiniciarCbs.add(fn)
  return () => onReiniciarCbs.delete(fn)
}
```

En la función `reiniciar()` existente, al final del cuerpo agregar:

```js
  for (const fn of onReiniciarCbs) { try { fn() } catch {} }
```

- [ ] **Step 3: Exportar las nuevas funciones**

En el objeto que devuelve `useGameState` (el `export const useGameState = () => ({ ... })`), agregar `registrarEvento`, `onEvento`, `onReiniciarPartida` a la lista de exports.

- [ ] **Step 4: Verificar sintaxis**

Run: `node --check "app/composables/useGameState.js"`
Expected: sin salida (OK).

- [ ] **Step 5: Commit** (supeditado al OK del usuario)

```bash
git add app/composables/useGameState.js
git commit -m "feat(logros): bus de eventos en useGameState"
```

---

## Task 2: Catálogo y store de useLogros (sin reglas todavía)

**Files:**
- Create: `app/composables/useLogros.js`
- Test: `docs/superpowers/tmp/test-logros.mjs` (temporal, se borra al final)

Primero el esqueleto: catálogo de 20, store por defecto, load/save con try/catch. Sin reglas aún. El catálogo es data pura, testeable.

- [ ] **Step 1: Escribir el test de la estructura del catálogo**

Create `docs/superpowers/tmp/test-logros.mjs`:

```js
// Test de la estructura del catálogo. Importa solo el array (data pura).
import assert from 'node:assert'
import { CATALOGO } from '../../../app/composables/logros-catalogo.mjs'

// 20 logros exactos
assert.equal(CATALOGO.length, 20, 'debe haber 20 logros')

// ids únicos
const ids = CATALOGO.map(l => l.id)
assert.equal(new Set(ids).size, 20, 'ids deben ser únicos')

// cada logro tiene los campos requeridos
for (const l of CATALOGO) {
  assert.ok(l.id && typeof l.id === 'string', `id válido: ${JSON.stringify(l)}`)
  assert.ok(l.nombre && typeof l.nombre === 'string', `nombre válido: ${l.id}`)
  assert.ok(l.desc && typeof l.desc === 'string', `desc válida: ${l.id}`)
  assert.ok(l.icono && typeof l.icono === 'string', `icono válido: ${l.id}`)
}

// el capstone y el completista existen
assert.ok(ids.includes('las_sabe_todas'), 'capstone presente')
assert.ok(ids.includes('completista'), 'completista presente')

console.log('OK: catálogo de logros válido (20, ids únicos, campos completos)')
```

> **Decisión de estructura**: el catálogo vive en un archivo `.mjs` separado
> (`app/composables/logros-catalogo.mjs`) para poder importarlo desde node sin Vue/Nuxt.
> `useLogros.js` lo re-importa. Es data pura, sin dependencias.

- [ ] **Step 2: Correr el test para verlo fallar**

Run: `node "docs/superpowers/tmp/test-logros.mjs"`
Expected: FAIL — "Cannot find module ... logros-catalogo.mjs".

- [ ] **Step 3: Crear el catálogo**

Create `app/composables/logros-catalogo.mjs`:

```js
// Catálogo de los 20 logros. Data pura (sin Vue) para poder testear/importar desde node.
// Cada logro: { id, nombre, desc, icono }. (campo `secreto` reservado, no usado en el MVP)
export const CATALOGO = [
  // Progresión / primera vez (fáciles)
  { id: 'primera_mano', nombre: 'Primera mano', desc: 'Ganá tu primera mano en cualquier juego.', icono: '🃏' },
  { id: 'primer_tropiezo', nombre: 'Primer tropiezo', desc: 'Perdé tu primera mano.', icono: '💸' },
  { id: 'timbero', nombre: 'Timbero de ley', desc: 'Probá los 5 minijuegos de la plaza.', icono: '🎲' },
  { id: 'conocido', nombre: 'Conocido en la plaza', desc: 'Hablá con los 11 vecinos de la plaza.', icono: '🗣️' },
  { id: 'libre_deuda', nombre: 'Libre de deuda', desc: 'Saldá la deuda con la mafia.', icono: '🕊️' },
  // Acumulado / grind (medios)
  { id: 'manos_calientes', nombre: 'Manos calientes', desc: 'Ganá 25 manos en total.', icono: '🔥' },
  { id: 'curtido', nombre: 'Curtido en las malas', desc: 'Perdé 25 manos en total.', icono: '🥃' },
  { id: 'bolsa_gorda', nombre: 'Bolsa gorda', desc: 'Llegá a tener $50 en la bolsa.', icono: '💰' },
  { id: 'patron', nombre: 'Patrón de la plaza', desc: 'Jugá 100 manos en total.', icono: '🎩' },
  { id: 'sin_mango', nombre: 'Sin un mango', desc: 'Quedate sin plata y caé en manos de la mafia.', icono: '☠️' },
  // Skill / por juego (medios-difíciles)
  { id: 'boca_sapo', nombre: 'Boca del sapo', desc: 'Embocá el sapo en la franja del x3.', icono: '🐸' },
  { id: 'linea_bingo', nombre: 'Línea y bingo, doña', desc: 'Cantá línea y bingo en la misma mano.', icono: '🎯' },
  { id: 'siete_clavado', nombre: 'Siete clavado', desc: 'Acertá el Exacto 7 en los dados (x4).', icono: '🎰' },
  { id: 'segui_bolita', nombre: 'Seguí la bolita', desc: 'Ganá en los cubiletes con la mesa difícil (nivel 3+).', icono: '🥤' },
  { id: 'las_sabe_todas', nombre: 'El que las sabe todas', desc: 'Ganá al menos una vez en los 5 minijuegos.', icono: '🏆' },
  // Racha / secreto / meta (difíciles)
  { id: 'en_racha', nombre: 'En racha', desc: 'Ganá 5 manos seguidas.', icono: '⚡' },
  { id: 'de_rodillas', nombre: 'De rodillas y de pie', desc: 'Tocá fondo ($1 o menos) y saldá la deuda igual.', icono: '🙌' },
  { id: 'audaz', nombre: 'Apostador audaz', desc: 'Ganá una mano apostando el máximo ($5).', icono: '🎏' },
  { id: 'maestro_garito', nombre: 'Maestro del garito', desc: 'Saldá la deuda con el sapo y los cubiletes en nivel 5+.', icono: '👑' },
  { id: 'completista', nombre: 'Completista', desc: 'Desbloqueá todos los demás logros.', icono: '💯' }
]
```

- [ ] **Step 4: Correr el test para verlo pasar**

Run: `node "docs/superpowers/tmp/test-logros.mjs"`
Expected: PASS — "OK: catálogo de logros válido".

- [ ] **Step 5: Commit** (supeditado al OK del usuario)

```bash
git add app/composables/logros-catalogo.mjs docs/superpowers/plans/2026-06-09-logros.md
git commit -m "feat(logros): catálogo de 20 logros"
```

---

## Task 3: Reglas de logros (lógica pura testeable)

**Files:**
- Create: `app/composables/logros-reglas.mjs`
- Test: `docs/superpowers/tmp/test-reglas.mjs` (temporal)

El corazón del sistema: una función pura que, dado el store actual + un evento (o snapshot de state), devuelve el store actualizado + la lista de ids recién desbloqueados. Pura = sin Vue, sin localStorage, sin tiempo. Testeable a fondo.

La firma:
```js
// estado del logro-store (lo que se persiste + racha en memoria)
// procesarEvento(store, evento) => { store, nuevos: [ids] }
// procesarState(store, snapshot) => { store, nuevos: [ids] }   // para plata/derrota
```

- [ ] **Step 1: Escribir los tests de las reglas**

Create `docs/superpowers/tmp/test-reglas.mjs`:

```js
import assert from 'node:assert'
import { storeInicial, procesarEvento, procesarState } from '../../../app/composables/logros-reglas.mjs'

function desbloqueados(r) { return Object.keys(r.store.desbloqueados) }

// --- primera_mano: primer gano ---
{
  let { store } = procesarEvento(storeInicial(), { tipo: 'jugo', juego: 'naipes' })
  const r = procesarEvento(store, { tipo: 'gano', juego: 'naipes', apuesta: 50, ganancia: 100 })
  assert.ok(r.nuevos.includes('primera_mano'), 'primera_mano al primer gano')
}

// --- primer_tropiezo: primer perdio ---
{
  const r = procesarEvento(storeInicial(), { tipo: 'perdio', juego: 'dados' })
  assert.ok(r.nuevos.includes('primer_tropiezo'), 'primer_tropiezo al primer perdio')
}

// --- timbero: 5 juegos jugados ---
{
  let store = storeInicial()
  for (const j of ['naipes', 'dados', 'cubiletes', 'sapo']) {
    store = procesarEvento(store, { tipo: 'jugo', juego: j }).store
  }
  const r = procesarEvento(store, { tipo: 'jugo', juego: 'bingo' })
  assert.ok(r.nuevos.includes('timbero'), 'timbero al jugar los 5')
}

// --- timbero NO se dispara con juegos repetidos ---
{
  let store = storeInicial()
  for (let i = 0; i < 5; i++) store = procesarEvento(store, { tipo: 'jugo', juego: 'naipes' }).store
  assert.ok(!desbloqueados({ store }).includes('timbero'), 'timbero requiere 5 distintos')
}

// --- conocido: 11 npcs distintos ---
{
  let store = storeInicial()
  for (let i = 0; i < 10; i++) store = procesarEvento(store, { tipo: 'hablo', npcId: 'npc' + i }).store
  const r = procesarEvento(store, { tipo: 'hablo', npcId: 'npc10' })
  assert.ok(r.nuevos.includes('conocido'), 'conocido al hablar con 11')
}

// --- manos_calientes: 25 ganadas ---
{
  let store = storeInicial()
  let last
  for (let i = 0; i < 25; i++) last = procesarEvento(store, { tipo: 'gano', juego: 'naipes', apuesta: 50, ganancia: 100 }), store = last.store
  assert.ok(last.nuevos.includes('manos_calientes') || desbloqueados(last).includes('manos_calientes'), 'manos_calientes a las 25')
  assert.equal(store.contadores.ganadas, 25, 'contador ganadas = 25')
}

// --- patron: 100 jugadas (gano o perdio cuentan) ---
{
  let store = storeInicial()
  for (let i = 0; i < 100; i++) {
    store = procesarEvento(store, { tipo: i % 2 ? 'gano' : 'perdio', juego: 'dados', apuesta: 50, ganancia: 100 }).store
  }
  assert.ok(desbloqueados({ store }).includes('patron'), 'patron a las 100 jugadas')
}

// --- audaz: gano apostando 500 ---
{
  const r = procesarEvento(storeInicial(), { tipo: 'gano', juego: 'sapo', apuesta: 500, ganancia: 1000 })
  assert.ok(r.nuevos.includes('audaz'), 'audaz al ganar con apuesta máx')
}
// audaz NO con apuesta menor
{
  const r = procesarEvento(storeInicial(), { tipo: 'gano', juego: 'sapo', apuesta: 300, ganancia: 600 })
  assert.ok(!r.nuevos.includes('audaz'), 'audaz solo con 500')
}

// --- boca_sapo / linea_bingo / siete_clavado ---
{
  assert.ok(procesarEvento(storeInicial(), { tipo: 'sapoX3' }).nuevos.includes('boca_sapo'), 'boca_sapo')
  assert.ok(procesarEvento(storeInicial(), { tipo: 'bingoDoble' }).nuevos.includes('linea_bingo'), 'linea_bingo')
  assert.ok(procesarEvento(storeInicial(), { tipo: 'dadosExacto7' }).nuevos.includes('siete_clavado'), 'siete_clavado')
}

// --- segui_bolita: cubiletes nivel >=3 ---
{
  assert.ok(procesarEvento(storeInicial(), { tipo: 'cubiletesNivel', nivel: 3 }).nuevos.includes('segui_bolita'), 'segui_bolita nivel 3')
  assert.ok(!procesarEvento(storeInicial(), { tipo: 'cubiletesNivel', nivel: 2 }).nuevos.includes('segui_bolita'), 'no en nivel 2')
}

// --- las_sabe_todas: ganar en los 5 ---
{
  let store = storeInicial()
  for (const j of ['naipes', 'dados', 'cubiletes', 'sapo']) {
    store = procesarEvento(store, { tipo: 'gano', juego: j, apuesta: 50, ganancia: 100 }).store
  }
  const r = procesarEvento(store, { tipo: 'gano', juego: 'bingo', apuesta: 50, ganancia: 100 })
  assert.ok(r.nuevos.includes('las_sabe_todas'), 'capstone al ganar en los 5')
}

// --- en_racha: 5 ganadas seguidas; se corta al perder ---
{
  let store = storeInicial()
  for (let i = 0; i < 4; i++) store = procesarEvento(store, { tipo: 'gano', juego: 'naipes', apuesta: 50, ganancia: 100 }).store
  const r = procesarEvento(store, { tipo: 'gano', juego: 'naipes', apuesta: 50, ganancia: 100 })
  assert.ok(r.nuevos.includes('en_racha'), 'en_racha a las 5 seguidas')
}
{
  let store = storeInicial()
  for (let i = 0; i < 4; i++) store = procesarEvento(store, { tipo: 'gano', juego: 'naipes', apuesta: 50, ganancia: 100 }).store
  store = procesarEvento(store, { tipo: 'perdio', juego: 'naipes' }).store // corta
  let last
  for (let i = 0; i < 4; i++) last = procesarEvento(store, { tipo: 'gano', juego: 'naipes', apuesta: 50, ganancia: 100 }), store = last.store
  assert.ok(!desbloqueados(last).includes('en_racha'), 'la racha se reinició tras perder')
}

// --- libre_deuda + de_rodillas + maestro_garito (evento saldo) ---
{
  // saldo simple → libre_deuda
  const r = procesarEvento(storeInicial(), { tipo: 'saldo', nivelSapo: 0, nivelCubiletes: 0 })
  assert.ok(r.nuevos.includes('libre_deuda'), 'libre_deuda al saldar')
}
{
  // tocó fondo antes (via procesarState plata<=100) y después saldó → de_rodillas
  let store = storeInicial()
  store = procesarState(store, { plata: 50, resultado: null }).store // marca tocoFondo
  const r = procesarEvento(store, { tipo: 'saldo', nivelSapo: 0, nivelCubiletes: 0 })
  assert.ok(r.nuevos.includes('de_rodillas'), 'de_rodillas: fondo + saldo')
}
{
  // saldó con ambos niveles >=5 → maestro_garito
  const r = procesarEvento(storeInicial(), { tipo: 'saldo', nivelSapo: 5, nivelCubiletes: 6 })
  assert.ok(r.nuevos.includes('maestro_garito'), 'maestro_garito con niveles altos')
}

// --- procesarState: bolsa_gorda (plata>=5000) y sin_mango (derrota) ---
{
  const r = procesarState(storeInicial(), { plata: 5000, resultado: null })
  assert.ok(r.nuevos.includes('bolsa_gorda'), 'bolsa_gorda a 5000¢')
}
{
  const r = procesarState(storeInicial(), { plata: 0, resultado: 'derrota' })
  assert.ok(r.nuevos.includes('sin_mango'), 'sin_mango en derrota')
}

// --- completista: al tener los otros 19 ---
{
  let store = storeInicial()
  // marcar a mano 19 ids (todos menos completista)
  const ids = ['primera_mano','primer_tropiezo','timbero','conocido','libre_deuda','manos_calientes','curtido','bolsa_gorda','patron','sin_mango','boca_sapo','linea_bingo','siete_clavado','segui_bolita','las_sabe_todas','en_racha','de_rodillas','audaz']
  for (const id of ids) store.desbloqueados[id] = true
  // falta uno (maestro_garito) → lo desbloqueamos vía evento y debe disparar completista también
  const r = procesarEvento(store, { tipo: 'saldo', nivelSapo: 5, nivelCubiletes: 5 })
  assert.ok(r.nuevos.includes('maestro_garito'), 'desbloquea el 19º')
  assert.ok(r.nuevos.includes('completista'), 'completista al juntar los 19')
}

// --- idempotencia: re-desbloquear no agrega a nuevos ---
{
  let { store } = procesarEvento(storeInicial(), { tipo: 'sapoX3' }) // boca_sapo ya
  const r = procesarEvento(store, { tipo: 'sapoX3' })
  assert.ok(!r.nuevos.includes('boca_sapo'), 'no re-dispara un logro ya obtenido')
}

console.log('OK: reglas de logros (todas las condiciones)')
```

- [ ] **Step 2: Correr el test para verlo fallar**

Run: `node "docs/superpowers/tmp/test-reglas.mjs"`
Expected: FAIL — "Cannot find module ... logros-reglas.mjs".

- [ ] **Step 3: Implementar las reglas**

Create `app/composables/logros-reglas.mjs`:

```js
// Lógica pura de logros: sin Vue, sin localStorage, sin tiempo. Testeable con node.
// El "store" tiene la parte persistida (desbloqueados, contadores, sets) + estado de
// partida transitorio (rachaActual, tocoFondo). El consumidor (useLogros) decide qué persiste.
import { CATALOGO } from './logros-catalogo.mjs'

const APUESTA_MAX = 500   // 500¢ = $5
const PLATA_BOLSA_GORDA = 5000 // 5000¢ = $50
const PLATA_FONDO = 100   // 100¢ = $1

export function storeInicial() {
  return {
    desbloqueados: {},
    contadores: { ganadas: 0, perdidas: 0, jugadas: 0, picoPlata: 0 },
    sets: { jugados: [], ganados: [], npcs: [] },
    // transitorios (no se persisten): se reinician en partida nueva
    rachaActual: 0,
    tocoFondo: false
  }
}

// Marca un id como desbloqueado si no lo estaba. Empuja a `nuevos`.
function desbloquear(store, id, nuevos) {
  if (store.desbloqueados[id]) return
  store.desbloqueados[id] = true
  nuevos.push(id)
}

function agregarUnico(arr, valor) {
  if (!arr.includes(valor)) arr.push(valor)
}

// Tras desbloquear cualquier cosa, revisar el completista (los OTROS 19).
function chequearCompletista(store, nuevos) {
  if (store.desbloqueados.completista) return
  const totalSinCompletista = CATALOGO.length - 1 // 19
  const hechos = CATALOGO.filter(l => l.id !== 'completista' && store.desbloqueados[l.id]).length
  if (hechos >= totalSinCompletista) desbloquear(store, 'completista', nuevos)
}

export function procesarEvento(store, evento) {
  const nuevos = []
  const c = store.contadores
  const s = store.sets

  switch (evento.tipo) {
    case 'jugo': {
      c.jugadas++
      agregarUnico(s.jugados, evento.juego)
      if (s.jugados.length >= 5) desbloquear(store, 'timbero', nuevos)
      if (c.jugadas >= 100) desbloquear(store, 'patron', nuevos)
      break
    }
    case 'gano': {
      c.ganadas++
      store.rachaActual++
      desbloquear(store, 'primera_mano', nuevos)
      agregarUnico(s.ganados, evento.juego)
      if (s.ganados.length >= 5) desbloquear(store, 'las_sabe_todas', nuevos)
      if (c.ganadas >= 25) desbloquear(store, 'manos_calientes', nuevos)
      if (store.rachaActual >= 5) desbloquear(store, 'en_racha', nuevos)
      if (evento.apuesta === APUESTA_MAX) desbloquear(store, 'audaz', nuevos)
      break
    }
    case 'perdio': {
      c.perdidas++
      store.rachaActual = 0
      desbloquear(store, 'primer_tropiezo', nuevos)
      if (c.perdidas >= 25) desbloquear(store, 'curtido', nuevos)
      break
    }
    case 'hablo': {
      agregarUnico(s.npcs, evento.npcId)
      if (s.npcs.length >= 11) desbloquear(store, 'conocido', nuevos)
      break
    }
    case 'sapoX3': desbloquear(store, 'boca_sapo', nuevos); break
    case 'bingoDoble': desbloquear(store, 'linea_bingo', nuevos); break
    case 'dadosExacto7': desbloquear(store, 'siete_clavado', nuevos); break
    case 'cubiletesNivel':
      if (evento.nivel >= 3) desbloquear(store, 'segui_bolita', nuevos)
      break
    case 'saldo': {
      desbloquear(store, 'libre_deuda', nuevos)
      if (store.tocoFondo) desbloquear(store, 'de_rodillas', nuevos)
      if (evento.nivelSapo >= 5 && evento.nivelCubiletes >= 5) desbloquear(store, 'maestro_garito', nuevos)
      break
    }
  }

  if (nuevos.length) chequearCompletista(store, nuevos)
  return { store, nuevos }
}

// Reglas derivadas de observar el state global (plata, resultado).
export function procesarState(store, snapshot) {
  const nuevos = []
  const c = store.contadores

  if (snapshot.plata > c.picoPlata) c.picoPlata = snapshot.plata
  if (snapshot.plata >= PLATA_BOLSA_GORDA) desbloquear(store, 'bolsa_gorda', nuevos)
  if (snapshot.plata <= PLATA_FONDO) store.tocoFondo = true
  if (snapshot.resultado === 'derrota') desbloquear(store, 'sin_mango', nuevos)

  if (nuevos.length) chequearCompletista(store, nuevos)
  return { store, nuevos }
}

// Corta la racha y el flag de fondo (partida nueva). Conserva desbloqueados/contadores/sets.
export function reiniciarPartida(store) {
  store.rachaActual = 0
  store.tocoFondo = false
}
```

- [ ] **Step 4: Correr el test para verlo pasar**

Run: `node "docs/superpowers/tmp/test-reglas.mjs"`
Expected: PASS — "OK: reglas de logros (todas las condiciones)".

- [ ] **Step 5: Commit** (supeditado al OK del usuario)

```bash
git add app/composables/logros-reglas.mjs
git commit -m "feat(logros): reglas puras + tests"
```

---

## Task 4: Composable useLogros (singleton reactivo)

**Files:**
- Create: `app/composables/useLogros.js`

Envuelve las reglas puras con: reactividad Vue, persistencia localStorage, suscripción al bus de `useGameState`, watcher de `state`, y cola de toasts. Init lazy (al primer `useLogros()`).

- [ ] **Step 1: Crear el composable**

Create `app/composables/useLogros.js`:

```js
// Sistema de logros. Singleton reactivo (como useGameState). Escucha el bus de eventos
// y observa el state para desbloquear logros, persiste en localStorage, encola toasts.
import { reactive, computed, watch } from 'vue'
import { CATALOGO } from './logros-catalogo.mjs'
import { storeInicial, procesarEvento, procesarState, reiniciarPartida } from './logros-reglas.mjs'
import { useGameState } from './useGameState'

const LS_KEY = 'plaza1810_logros_v1'

function load() {
  if (typeof window === 'undefined') return null
  try {
    const raw = localStorage.getItem(LS_KEY)
    return raw ? JSON.parse(raw) : null
  } catch { return null }
}

function persistir() {
  if (typeof window === 'undefined') return
  try {
    // Solo la parte persistente (no racha/tocoFondo, que son de partida).
    localStorage.setItem(LS_KEY, JSON.stringify({
      desbloqueados: store.desbloqueados,
      contadores: store.contadores,
      sets: store.sets
    }))
  } catch {}
}

// store reactivo: arranca del default + lo guardado (si hay).
const guardado = load()
const base = storeInicial()
const store = reactive({
  ...base,
  desbloqueados: guardado?.desbloqueados ?? base.desbloqueados,
  contadores: { ...base.contadores, ...(guardado?.contadores ?? {}) },
  sets: {
    jugados: guardado?.sets?.jugados ?? [],
    ganados: guardado?.sets?.ganados ?? [],
    npcs: guardado?.sets?.npcs ?? []
  }
})

// Cola de toasts: ids pendientes de mostrar.
const colaToasts = reactive([])

function encolar(ids) {
  for (const id of ids) colaToasts.push(id)
}

let iniciado = false
function iniciar() {
  if (iniciado || typeof window === 'undefined') return
  iniciado = true
  const { onEvento, onReiniciarPartida, state } = useGameState()

  // Eventos del juego.
  onEvento((evento) => {
    const { nuevos } = procesarEvento(store, evento)
    if (nuevos.length) { encolar(nuevos); persistir() }
  })

  // Observar state (plata, resultado) para los logros derivados.
  watch(
    () => ({ plata: state.plata, resultado: state.resultado }),
    (snap) => {
      const { nuevos } = procesarState(store, snap)
      if (nuevos.length) { encolar(nuevos); persistir() }
    },
    { immediate: true }
  )

  // Partida nueva: cortar racha y flag de fondo (no toca logros/contadores).
  onReiniciarPartida(() => reiniciarPartida(store))
}

const progreso = computed(() => ({
  hechos: CATALOGO.filter(l => store.desbloqueados[l.id]).length,
  total: CATALOGO.length
}))

const toastActual = computed(() => {
  const id = colaToasts[0]
  if (!id) return null
  return CATALOGO.find(l => l.id === id) ?? null
})

function descartarToast() {
  colaToasts.shift()
}

export const useLogros = () => {
  iniciar()
  return {
    catalogo: CATALOGO,
    desbloqueados: store.desbloqueados,
    progreso,
    toastActual,
    descartarToast
  }
}
```

- [ ] **Step 2: Verificar sintaxis**

Run: `node --check "app/composables/useLogros.js"`
Expected: sin salida (OK).

> Nota: `node --check` no resuelve imports de Vue; solo valida sintaxis. La integración real
> se prueba en el build y la validación visual.

- [ ] **Step 3: Commit** (supeditado al OK del usuario)

```bash
git add app/composables/useLogros.js
git commit -m "feat(logros): composable useLogros (singleton + cola)"
```

---

## Task 5: Cablear los minijuegos

**Files:**
- Modify: `app/components/minigames/DiceGame.vue`
- Modify: `app/components/minigames/CardsGame.vue`
- Modify: `app/components/minigames/CupsGame.vue`
- Modify: `app/components/minigames/SapoGame.vue`
- Modify: `app/components/minigames/BingoGame.vue`

Cada minijuego emite `jugo` al apostar, `gano`/`perdio` al resolver, + el evento específico. `registrarEvento` viene de `useGameState`. **Importante**: el evento `gano` lleva `{ juego, apuesta, ganancia }` y `jugo` lleva `{ juego }`.

> Para cada archivo: en el `useGameState()` destructurado, **agregar `registrarEvento`** a lo
> que ya se importa. Buscar la línea `const { ... } = useGameState()` y sumar `registrarEvento`.

- [ ] **Step 1: DiceGame — apostar, gano/perdio, exacto7**

En `app/components/minigames/DiceGame.vue`:

1. Agregar `registrarEvento` al destructuring de `useGameState()`.
2. Justo después de `if (!apostar(apuesta.value)) return` (línea ~41), agregar:
```js
  registrarEvento('jugo', { juego: 'dados' })
```
3. En la rama de victoria (donde está `cobrar(ganancia)`, línea ~92), después de `cobrar(ganancia)`:
```js
    registrarEvento('gano', { juego: 'dados', apuesta: apuesta.value, ganancia })
    if (prediccion.value === 'exacto') registrarEvento('dadosExacto7', {})
```
4. En la rama de derrota (donde está `cobrar(0)`, línea ~95), después:
```js
    registrarEvento('perdio', { juego: 'dados' })
```

Run: `node --check "app/components/minigames/DiceGame.vue"` → debe fallar (es .vue, no JS puro). En su lugar verificar en el build final (Task 9). Por ahora, revisión visual del diff.

- [ ] **Step 2: CardsGame — apostar, gano/perdio**

En `app/components/minigames/CardsGame.vue`:

1. Agregar `registrarEvento` al destructuring de `useGameState()`.
2. Después de `if (!apostar(apuesta.value)) return` (línea ~46):
```js
  registrarEvento('jugo', { juego: 'naipes' })
```
3. Donde resuelve la serie (`cobrar(ganancia)` línea ~170, victoria):
```js
    registrarEvento('gano', { juego: 'naipes', apuesta: apuesta.value, ganancia })
```
4. En el empate (`cobrar(apuesta.value)` línea ~174) **no** emitir gano ni perdio (no ganaste ni perdiste; la mano no cuenta como ganada). Dejar sin evento de resultado.
5. En la derrota (`cobrar(0)` línea ~177):
```js
    registrarEvento('perdio', { juego: 'naipes' })
```

> Verificar el nombre real de la variable de ganancia en CardsGame (puede no llamarse
> `ganancia`); usar la que corresponda al monto cobrado en la rama de victoria.

- [ ] **Step 3: CupsGame — apostar, gano/perdio, nivel**

En `app/components/minigames/CupsGame.vue`:

1. Agregar `registrarEvento` al destructuring.
2. Después de `if (!apostar(apuesta.value)) return` (línea ~42):
```js
  registrarEvento('jugo', { juego: 'cubiletes' })
```
3. En victoria — **antes** de `subirNivel('cubiletes')** (línea ~134), para capturar el nivel
   con el que se ganó (no el ya subido):
```js
    registrarEvento('gano', { juego: 'cubiletes', apuesta: apuesta.value, ganancia })
    registrarEvento('cubiletesNivel', { nivel: state.nivel.cubiletes })
```
   (dejar `subirNivel('cubiletes')` después, como está).
4. En derrota (`cobrar(0)` línea ~138):
```js
    registrarEvento('perdio', { juego: 'cubiletes' })
```

- [ ] **Step 4: SapoGame — apostar, gano/perdio, x3**

En `app/components/minigames/SapoGame.vue`:

1. Agregar `registrarEvento` al destructuring (ya importa `state, apostar, cobrar, subirNivel, reiniciarNivel`).
2. Después de `if (!apostar(apuesta.value)) return` (línea ~45):
```js
  registrarEvento('jugo', { juego: 'sapo' })
```
3. En victoria (`cobrar(ganancia)` línea ~75, antes/después de `subirNivel('sapo')`):
```js
    registrarEvento('gano', { juego: 'sapo', apuesta: apuesta.value, ganancia })
    if (res.pago >= pagoMax) registrarEvento('sapoX3', {})
```
   (`pagoMax` ya está definido en el componente, línea ~29.)
4. En derrota (`cobrar(0)` línea ~82):
```js
    registrarEvento('perdio', { juego: 'sapo' })
```

- [ ] **Step 5: BingoGame — apostar, gano/perdio, doble**

En `app/components/minigames/BingoGame.vue`:

1. Agregar `registrarEvento` al destructuring.
2. Después de `if (!apostar(apuesta.value)) return` (línea ~54):
```js
  registrarEvento('jugo', { juego: 'bingo' })
```
3. Donde arma `resultado.value` tras `cobrar(ganancia)` (línea ~202-205). Después de setear
   `resultado.value`, emitir según resultado:
```js
    if (resultado.value.gano) {
      registrarEvento('gano', { juego: 'bingo', apuesta: apuesta.value, ganancia })
      if (resultado.value.ganasteLinea && resultado.value.ganasteBingo) registrarEvento('bingoDoble', {})
    } else {
      registrarEvento('perdio', { juego: 'bingo' })
    }
```
   > Verificar el nombre de la variable de ganancia en BingoGame (cómo se llama el monto
   > pasado a `cobrar`). Usar esa. `resultado.value.gano/ganasteLinea/ganasteBingo` ya existen.

- [ ] **Step 6: Commit** (supeditado al OK del usuario)

```bash
git add app/components/minigames/DiceGame.vue app/components/minigames/CardsGame.vue app/components/minigames/CupsGame.vue app/components/minigames/SapoGame.vue app/components/minigames/BingoGame.vue
git commit -m "feat(logros): emitir eventos desde los 5 minijuegos"
```

---

## Task 6: Cablear NPCs (evento hablo) y garito (evento saldo)

**Files:**
- Modify: `app/game/plaza/PlazaScene.js:158-162`
- Modify: `app/game/interiors/InteriorScene.js:105-109`
- Modify: `app/components/scenes/PlazaScene.vue`
- Modify: `app/components/scenes/InteriorScene.vue`
- Modify: `app/components/scenes/GaritoScene.vue`

Los renderers devuelven `{nombre, texto}` desde `hablar()`. Hay que sumar `id` para poder
emitir `hablo {npcId}` desde la capa Vue (los renderers no importan composables; patrón del
proyecto).

- [ ] **Step 1: PlazaScene.js — hablar() devuelve id**

En `app/game/plaza/PlazaScene.js`, método `hablar(n)` (línea ~158-162), cambiar el return:
```js
    return { id: n.id, nombre: n.nombre, texto }
```

- [ ] **Step 2: InteriorScene.js — hablar() devuelve id**

En `app/game/interiors/InteriorScene.js`, método `hablar(n)` (línea ~105-109), cambiar el return:
```js
    return { id: n.id, nombre: n.nombre, texto }
```

- [ ] **Step 3: Verificar sintaxis de los renderers**

Run: `node --check "app/game/plaza/PlazaScene.js" && node --check "app/game/interiors/InteriorScene.js"`
Expected: sin salida (OK).

- [ ] **Step 4: PlazaScene.vue — emitir hablo**

En `app/components/scenes/PlazaScene.vue`:
1. Agregar `registrarEvento` al destructuring de `useGameState()` (verificar que ya se importa
   `useGameState`; si no, importarlo: `import { useGameState } from '~/composables/useGameState'`
   y `const { registrarEvento } = useGameState()`).
2. Donde se abre el diálogo a partir del dato del renderer. Hay dos vías:
   - `renderer.onHablar = (d) => { ... }` (línea ~56): dentro, al recibir `d`, agregar
     `if (d?.id) registrarEvento('hablo', { npcId: d.id })`.
   - La vía `hablarConCerca()` (línea ~117): el resultado `d` se usa para abrir el diálogo;
     en ese punto agregar el mismo `if (d?.id) registrarEvento('hablo', { npcId: d.id })`.

   > Centralizar: si ambas vías terminan llamando a una sola función que setea el diálogo
   > (ej. `abrirDialogo(d)`), poner el `registrarEvento` ahí una sola vez. Inspeccionar el
   > componente y elegir el único punto por el que pasan los dos caminos.

- [ ] **Step 5: InteriorScene.vue — emitir hablo**

En `app/components/scenes/InteriorScene.vue`:
1. Agregar `registrarEvento` (mismo patrón que arriba).
2. En el flujo `interactuar()` / `hablarConCerca` (línea ~61, donde el dato del NPC abre el
   diálogo), en el único punto por el que pasan todos los caminos de "se abrió un diálogo de NPC":
   `if (d?.id) registrarEvento('hablo', { npcId: d.id })`.

- [ ] **Step 6: GaritoScene.vue — emitir saldo**

En `app/components/scenes/GaritoScene.vue`:
1. Agregar `registrarEvento` al destructuring de `useGameState()`.
2. Donde se llama `saldarDeuda()` con éxito (la acción del botón "Saldar"), justo después de
   que `saldarDeuda()` devuelve true / antes de `irAPlazaConCinematica('victoria')`, agregar:
```js
    registrarEvento('saldo', { nivelSapo: state.nivel.sapo, nivelCubiletes: state.nivel.cubiletes })
```
   > Verificar que `state` esté disponible en el destructuring; si no, agregarlo. El `saldo`
   > debe emitirse SOLO en el saldado real (no en los botones DEV GANAR/PERDER, que no saldan
   > la deuda de verdad — esos van directo a la cinemática sin tocar la plata).

- [ ] **Step 7: Commit** (supeditado al OK del usuario)

```bash
git add app/game/plaza/PlazaScene.js app/game/interiors/InteriorScene.js app/components/scenes/PlazaScene.vue app/components/scenes/InteriorScene.vue app/components/scenes/GaritoScene.vue
git commit -m "feat(logros): emitir eventos hablo (NPCs) y saldo (garito)"
```

---

## Task 7: Toast de logro con cola

**Files:**
- Create: `app/components/ui/ToastLogro.vue`
- Modify: `app/components/GameRoot.vue`

Toast estilo `AvisoSaldar` pero leyendo `toastActual` y avanzando la cola. Acento dorado/farol
(no verde, para diferenciarlo del aviso de saldar).

- [ ] **Step 1: Crear ToastLogro.vue**

Create `app/components/ui/ToastLogro.vue`:

```vue
<script setup>
import { ref, watch, onUnmounted } from 'vue'
import { useLogros } from '~/composables/useLogros'

const { toastActual, descartarToast } = useLogros()

const visible = ref(false)
let timer = null

// Cada vez que hay un logro al frente de la cola, mostrarlo ~4.5s y luego avanzar.
watch(
  toastActual,
  (logro) => {
    if (logro && !visible.value) mostrar()
  },
  { immediate: true }
)

function mostrar() {
  visible.value = true
  clearTimeout(timer)
  timer = setTimeout(cerrar, 4500)
}

function cerrar() {
  visible.value = false
  // Esperar a que termine la transición de salida antes de avanzar la cola.
  setTimeout(() => {
    descartarToast()
    // Si quedan más en la cola, el watch los toma; por las dudas, re-chequear.
    if (toastActual.value) mostrar()
  }, 420)
}

onUnmounted(() => clearTimeout(timer))
</script>

<template>
  <Transition name="logro">
    <div
      v-if="visible && toastActual"
      class="flex items-center gap-3.5 absolute top-6 left-1/2 -translate-x-1/2 z-40 bg-noche/95 border-2 border-farol/60 rounded-xl shadow-farol-lg px-6 py-3.5"
    >
      <span class="text-3xl leading-none">{{ toastActual.icono }}</span>
      <div class="flex flex-col">
        <span class="font-display text-dorado text-xs uppercase tracking-[0.2em]">¡Logro desbloqueado!</span>
        <span class="font-display text-light text-lg tracking-wide">{{ toastActual.nombre }}</span>
      </div>
    </div>
  </Transition>
</template>

<style scoped>
.logro-enter-active,
.logro-leave-active {
  transition: all 0.4s ease;
}
.logro-enter-from,
.logro-leave-to {
  opacity: 0;
  transform: translate(-50%, -12px);
}
</style>
```

- [ ] **Step 2: Montar ToastLogro en GameRoot**

En `app/components/GameRoot.vue`, dentro del frame de juego, junto a `<AvisoSaldar />` (línea ~45):
```html
      <AvisoSaldar />
      <ToastLogro />
```
(Nuxt auto-importa componentes; no hace falta import explícito.)

- [ ] **Step 3: Verificar build parcial**

Run: `npx nuxt build` (desde la raíz del proyecto)
Expected: build OK (o si tarda, al menos que no falle por estos archivos). Si falla, leer el
error y corregir.

- [ ] **Step 4: Commit** (supeditado al OK del usuario)

```bash
git add app/components/ui/ToastLogro.vue app/components/GameRoot.vue
git commit -m "feat(logros): toast con cola"
```

---

## Task 8: Galería de logros (modal) + botón en TopBar

**Files:**
- Create: `app/components/ui/GaleriaLogros.vue`
- Modify: `app/components/ui/TopBar.vue`
- Modify: `app/components/GameRoot.vue`

- [ ] **Step 1: Crear GaleriaLogros.vue**

Create `app/components/ui/GaleriaLogros.vue`:

```vue
<script setup>
import { onMounted, onUnmounted } from 'vue'
import { useLogros } from '~/composables/useLogros'

const emit = defineEmits(['cerrar'])
const { catalogo, desbloqueados, progreso } = useLogros()

function onEsc(e) {
  if (e.key === 'Escape') emit('cerrar')
}
onMounted(() => window.addEventListener('keydown', onEsc))
onUnmounted(() => window.removeEventListener('keydown', onEsc))
</script>

<template>
  <div
    class="flex justify-center items-center absolute inset-0 z-50 bg-noche/80 backdrop-blur-sm"
    @click.self="emit('cerrar')"
  >
    <div class="w-[720px] max-h-[680px] flex flex-col gap-5 bg-noche-2 border-2 border-farol/40 rounded-2xl shadow-farol-lg p-8">
      <div class="flex justify-between items-center">
        <h3 class="font-display text-3xl text-dorado tracking-wide">Logros</h3>
        <div class="flex items-center gap-4">
          <span class="font-display text-xl text-farol/80 tabular-nums">{{ progreso.hechos }}/{{ progreso.total }}</span>
          <button
            class="size-9 flex justify-center items-center bg-noche border border-light/20 hover:border-farol/50 rounded-lg text-light/70 hover:text-dorado transition-colors"
            @click="emit('cerrar')"
          >
            ✕
          </button>
        </div>
      </div>

      <div class="grid grid-cols-2 gap-3 overflow-y-auto pr-1">
        <div
          v-for="l in catalogo"
          :key="l.id"
          class="flex items-start gap-3 border rounded-xl transition-colors p-3.5"
          :class="desbloqueados[l.id]
            ? 'bg-farol/10 border-farol/40'
            : 'bg-noche/50 border-light/10'"
        >
          <span
            class="text-3xl leading-none"
            :class="desbloqueados[l.id] ? '' : 'grayscale opacity-40'"
          >{{ l.icono }}</span>
          <div class="flex flex-col gap-0.5">
            <span
              class="font-display text-base tracking-wide"
              :class="desbloqueados[l.id] ? 'text-dorado' : 'text-light/40'"
            >{{ l.nombre }}</span>
            <span
              class="font-cuerpo text-sm leading-snug"
              :class="desbloqueados[l.id] ? 'text-light/80' : 'text-light/35'"
            >{{ l.desc }}</span>
          </div>
        </div>
      </div>

      <p class="font-cuerpo text-base text-light/40 text-center italic">ESC o clic afuera para cerrar</p>
    </div>
  </div>
</template>
```

- [ ] **Step 2: Botón 🏆 en TopBar**

En `app/components/ui/TopBar.vue`, junto al botón "?" existente (que emite `ayuda`), agregar un
botón hermano que emita `logros`. Primero ubicar el botón de ayuda y replicar su estilo. Agregar
`'logros'` al `defineEmits`. Ejemplo del botón (ajustar clases al estilo real del botón "?"):
```html
        <button
          class="size-9 flex justify-center items-center bg-noche border border-light/20 hover:border-farol/50 rounded-lg text-light/70 hover:text-dorado transition-colors"
          aria-label="Ver logros"
          @click="emit('logros')"
        >
          🏆
        </button>
```
> Inspeccionar TopBar.vue para ver cómo está hecho el botón "?" (clases, ubicación en el layout)
> y poner el de logros al lado, con el mismo tamaño/estilo. Confirmar que `defineEmits` incluya
> `['ayuda', 'logros']`.

- [ ] **Step 3: Togglear GaleriaLogros en GameRoot**

En `app/components/GameRoot.vue`:
1. Agregar un ref `const galeriaAbierta = ref(false)` junto a `ayudaAbierta`.
2. En `<TopBar>`, sumar el listener: `@logros="galeriaAbierta = true"` (junto a `@ayuda=...`).
3. Junto a `<ControlesAyuda ... />` (línea ~48), agregar:
```html
      <GaleriaLogros v-if="galeriaAbierta" @cerrar="galeriaAbierta = false" />
```

- [ ] **Step 4: Verificar build**

Run: `npx nuxt build`
Expected: build OK.

- [ ] **Step 5: Commit** (supeditado al OK del usuario)

```bash
git add app/components/ui/GaleriaLogros.vue app/components/ui/TopBar.vue app/components/GameRoot.vue
git commit -m "feat(logros): galería modal + botón en TopBar"
```

---

## Task 9: Verificación final + limpieza

**Files:**
- Delete: `docs/superpowers/tmp/test-logros.mjs`, `docs/superpowers/tmp/test-reglas.mjs`

- [ ] **Step 1: Correr los tests puros una última vez**

Run: `node "docs/superpowers/tmp/test-logros.mjs" && node "docs/superpowers/tmp/test-reglas.mjs"`
Expected: ambos PASS.

- [ ] **Step 2: Build completo**

Run: `npx nuxt build`
Expected: build OK, sin errores.

- [ ] **Step 3: Validación visual (la hace el usuario en su dev server)**

Checklist para el usuario:
- Ganar una mano → toast "Primera mano".
- Ganar naipes la 1ª vez → ver que NO hay pop-spam (solo "Primera mano"; ya no hay logro por-juego).
- Abrir la galería con 🏆 en la TopBar → ver grid 20, contador X/20, bloqueados en gris.
- Embocar el x3 del sapo → toast "Boca del sapo".
- Hablar con los 11 NPCs → toast "Conocido en la plaza".
- Saldar la deuda → toast "Libre de deuda" (+ "De rodillas y de pie" si tocaste $1).
- Empezar partida nueva → los logros y contadores siguen; la racha se cortó.

- [ ] **Step 4: Borrar los tests temporales**

```bash
rm docs/superpowers/tmp/test-logros.mjs docs/superpowers/tmp/test-reglas.mjs
```
(Si `docs/superpowers/tmp/` queda vacío, borrar la carpeta.)

- [ ] **Step 5: Commit final** (supeditado al OK del usuario)

```bash
git add -A
git commit -m "chore(logros): limpieza de tests temporales"
```

---

## Resumen de cobertura del spec

- 20 logros con disparadores reales → Tasks 2, 3 (catálogo + reglas), verificados con tests.
- Bus de eventos en useGameState → Task 1.
- Composable useLogros (store + persistencia + cola + watchers) → Task 4.
- Eventos desde minijuegos → Task 5. Desde NPCs y garito → Task 6.
- Toast con cola → Task 7. Galería modal + botón TopBar → Task 8.
- Persistencia entre partidas + racha transitoria → Tasks 1 (onReiniciarPartida), 3 (reiniciarPartida), 4 (no persiste racha).
- Verificación (tests puros + build + visual) → Task 9.
